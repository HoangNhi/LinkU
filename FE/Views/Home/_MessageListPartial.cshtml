@using MODELS.BASE;
@model GetListPagingResponse
<div class="row">
    @foreach (var item in (List<MODELS.MESSAGE.Dtos.MODELMessage>)Model.Data)
    {
        <div class="card col-md-3">
            <div class="card-body">
                <h5 class="card-title">Hello</h5>
                <p class="card-text">@item.Content</p>
                <button type="button" class="btn btn-primary" data-mdb-ripple-init>Tham gia</button>
            </div>
        </div>
    }
</div>

